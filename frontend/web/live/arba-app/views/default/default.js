'use strict';

angular.module('arbaApp.default', [])

/*.config(function($routeProvider) {
	
	$routeProvider.when('/home', {
		templateUrl: 'arba-app/pages/default/default.html',
		controller: 'HomeCTRL'
	});
	
})*/

.controller('HomeCTRL', function($scope, $arbaSocket, $log, $interval) {
	
	
	
});